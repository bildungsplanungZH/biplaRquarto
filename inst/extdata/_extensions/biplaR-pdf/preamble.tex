%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% preamble.tex
% document preamble for a specific report
%
% use with documentclass biplaRartcl >= 2.0
% copy to report directory, edit as needed
%
% Author: Flavian Imlig <flavian.imlig@bi.zh.ch>
% Date: 24.06.2021
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%%%%%
% Document information
%%%%%

\subject{Titel in Kopfzeile anpassen} % shows in every header of standard page

%%%%%
% Titlepage and Impressum settings
%%%%%

\csdef{cl_tp_type}{spike} % titlepage design, values: flag (default) or spike
\csdef{cl_tp_fontsize}{Large} % font size for title and subtitle, values: large, Large, LARGE (default), huge, Huge
%\csdef{cl_tp_headtext}{abk} % content of titlepage header, defaults to Kanton Zürich\\Bildungsdirektion\\Bildungsplanung
%\csdef{cl_tp_titleprefix}{\hspace*{-1em}%} % code to be entered before the title, defaults to \@empty
\csdef{cl_tp_comment}{\textcolor{red}{Klassifikation: Intern}} % content of titlepage comment, defaults to \@empty
\toggletrue{cl_tp_date} % print date on titlepage, defaults to false

% \togglefalse{cl_tp_impressum} % use impressum page, defaults to true

%\publishers{Bildungsdirektion Kanton Zürich, Bildungsplanung} % defaults to Bildungsdirektion Kanton Zürich, Bildungsplanung

%\csdef{cl_tp_imp_author_appendix}{unter Mitarbeit von ABC} % shows only if set
%\csdef{cl_tp_imp_addition}{\subsubsection*{Illustrationen} Sehr schön} % shows only if set

%\togglefalse{cl_tp_imp_address} % show address in impressum, defaults to true
%\csdef{cl_tp_imp_email}{ab} % defaults to bildungsplanung@bi.zh.ch
%\csdef{cl_tp_imp_url}{\url{https://www.zh.ch}} % defaults to www.zh.ch/studien-bildung, www.zh.ch/bildungsplanung

%\csdef{cl_tp_imp_citation}{Empfohlene Zitation gemäss APA} % shows only if set

%\toggletrue{cl_tp_imp_copyright} % print copyright disclaimer, defaults to true
%\csdef{cl_tp_imp_copyright_date}{April 2021} % defaults to date in Rmd header

%\csdef{cl_tp_imp_foot}{gedruckt auf Balance Pure, 100\% Recycling-Papier} % shows only if set

% \csdef{pdftitle}{Titel} % defaults to title
% \csdef{pdfauthor}{$author$} % defaults to publishers, set $author$ to use authors from yaml header

%%%%%
% Document settings
%%%%%

%\togglefalse{cl_header} % use header of biplaRartcl class, defaults to true

%\togglefalse{cl_colours} % use colour definitions, defaults to true
%\togglefalse{cl_penalties} % use penalty definitions for page layout, defaults to true
    %\brokenpenalty=10000 % override class penalty definition
%\togglefalse{cl_hyphenation} % use hyphsubst package with ngerman-x-latest, defaults to true

%\toggletrue{acrolist} % use acronym macros from package acro, defaults to false

\def\tightlist{}

\usepackage{tabularx, booktabs}
